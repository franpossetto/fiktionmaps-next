/** Fallback image when a fiction cover is missing or fails to load */
export const DEFAULT_FICTION_COVER = "/placeholder.svg"

/** Default accent for fiction badges/chips when no custom color is set */
export const DEFAULT_FICTION_ACCENT = "#64748b"
