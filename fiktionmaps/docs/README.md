# Project documentation

- **`reference/`** — current system truth (architecture, roles, places, cache, SEO).
- **`plans/`** — feature notes and implementation proposals. Not source of truth.

Agent entrypoint: `AGENTS.md` at the repo root.
Plans do not override `reference/` or `.cursor/rules/`.
