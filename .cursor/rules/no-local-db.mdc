User does not use a local Supabase database. The project is connected directly to production. NEVER run 'supabase db reset' or any destructive database commands, as this will wipe the production database.
